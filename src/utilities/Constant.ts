export const CHUNK_SIZE = 1024 * 1024 * 10; // 10MB, This must be bigger than or equal to 5MB, otherwise AWS will respond with: "Your proposed upload is smaller than the minimum allowed size"
export const API_KEY = "";
export const STAGING_URL = "";
export const PROCESS_URL = "";
export const STATUS_URL = "";
